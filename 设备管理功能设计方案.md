# 设备管理功能设计方案

## 1. 概述

为 RuoYi-Vue3-FastAPI 系统增加设备管理功能模块，实现设备的增删改查、状态管理、设备分组等功能。

## 2. 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | FastAPI 0.128.2 + SQLAlchemy 2.0.46 |
| 数据库 | PostgreSQL |
| 前端框架 | Vue 3 + Element Plus |
| 状态管理 | Pinia |

## 3. 后端设计

### 3.1 模块结构

```
ruoyi-fastapi-backend/module_device/
├── controller/device_controller.py   # 控制器层
├── service/device_service.py         # 服务层
├── dao/device_dao.py                 # 数据访问层
└── entity/
    ├── do/device_do.py               # 数据对象
    └── vo/device_vo.py               # 视图对象
```

### 3.2 数据库设计

**设备表 (sys_device)**

| 字段 | 类型 | 说明 |
|------|------|------|
| device_id | bigserial | 主键 |
| device_name | varchar(64) | 设备名称 |
| device_code | varchar(64) | 设备编码(唯一) |
| device_type | varchar(32) | 设备类型 |
| device_model | varchar(64) | 设备型号 |
| status | char(1) | 状态(0正常 1离线 2故障) |
| ip_address | varchar(128) | IP地址 |
| mac_address | varchar(64) | MAC地址 |
| location | varchar(255) | 设备位置 |
| department_id | bigint | 所属部门ID(外键) |
| user_id | bigint | 负责人ID(外键) |

**设备分组表 (sys_device_group)**

| 字段 | 类型 | 说明 |
|------|------|------|
| group_id | bigserial | 主键 |
| group_name | varchar(64) | 分组名称 |
| parent_id | bigint | 父分组ID |
| order_num | int | 显示顺序 |

**设备分组关联表 (sys_device_group_rel)**

| 字段 | 类型 | 说明 |
|------|------|------|
| device_id | bigint | 设备ID(外键) |
| group_id | bigint | 分组ID(外键) |

### 3.3 API设计

**设备型号接口** (`/device/model`)

| 方法 | 路径 | 权限 | 功能 |
|------|------|------|------|
| GET | /list | device:model:list | 获取列表 |
| GET | /get/{id} | device:model:query | 获取详情 |
| POST | /add | device:model:add | 新增 |
| PUT | /edit | device:model:edit | 修改 |
| DELETE | /delete/{id} | device:model:remove | 删除 |
| PUT | /status/{id}/{status} | device:model:edit | 修改状态 |

**设备分组接口** (`/device/group`)

| 方法 | 路径 | 权限 | 功能 |
|------|------|------|------|
| GET | /list | device:group:list | 获取列表 |
| GET | /tree | device:group:list | 获取树形结构 |
| POST | /add | device:group:add | 新增 |
| PUT | /edit | device:group:edit | 修改 |
| DELETE | /delete/{id} | device:group:remove | 删除 |

## 4. 前端设计

### 4.1 模块结构

```
src/modules/device/
├── api/index.js           # API接口
└── views/
    ├── index.vue          # 设备型号页面
    └── group/index.vue    # 设备分组页面
```

### 4.2 菜单配置

| 菜单名称 | 路由地址 | 组件路径 | 权限标识 |
|---------|---------|---------|---------|
| 设备管理 | device | - | - |
| 设备型号 | deviceModel | device/views/index | device:model:list |
| 设备分组 | deviceGroup | device/views/group/index | device:group:list |

## 5. 权限命名规范

```
模块:功能:操作
device:model:list   # 设备型号列表
device:model:query  # 设备型号查询
device:model:add    # 设备型号新增
device:model:edit   # 设备型号修改
device:model:remove # 设备型号删除

device:group:list   # 设备分组列表
device:group:add    # 设备分组新增
device:group:edit   # 设备分组修改
device:group:remove # 设备分组删除
```

## 6. 部署步骤

### 6.1 后端部署

```bash
# 1. 执行SQL脚本
docker cp sql/device.sql device_postgres:/tmp/
docker exec device_postgres psql -U device_user -d ruoyi_fastapi -f /tmp/device.sql

# 2. 执行菜单SQL
docker cp sql/device_menu_final.sql device_postgres:/tmp/
docker exec device_postgres psql -U device_user -d ruoyi_fastapi -f /tmp/device_menu_final.sql

# 3. 重启后端服务
python app.py --env=dev
```

### 6.2 前端部署

```bash
# 1. 启动前端服务
npm run dev

# 2. 清除浏览器缓存并重新登录
```

## 7. 文件清单

| 类型 | 文件路径 |
|------|---------|
| SQL | `sql/device.sql` - 数据库表结构 |
| SQL | `sql/device_menu_final.sql` - 菜单配置 |
| 后端 | `module_device/controller/device_controller.py` |
| 后端 | `module_device/service/device_service.py` |
| 后端 | `module_device/dao/device_dao.py` |
| 后端 | `module_device/entity/do/device_do.py` |
| 后端 | `module_device/entity/vo/device_vo.py` |
| 前端 | `src/modules/device/api/index.js` |
| 前端 | `src/modules/device/views/index.vue` |

## 8. 注意事项

1. **路由地址唯一性**：路由地址不能与其他模块重复，建议使用 `模块+功能` 命名（如 `deviceModel`）
2. **外键约束**：DO 文件中的 `ForeignKey` 必须正确配置，否则 SQLAlchemy 会报错
3. **权限标识**：前端 `v-hasPermi` 指令中的权限标识必须与后端一致
4. **API路径**：前端 API 路径必须与后端路由前缀一致
